$numbersArray = @(1, 5, 10, 15, 20)

# Number to find
$targetNumber = 8

# Check if the number is in the array
if ($numbersArray -contains $targetNumber) {
    Write-Host "Number $targetNumber found in the array."
} else {
    throw "Number $targetNumber not found in the array."
}
