# Initialize a variable to store the result
$greaterNumber = $null

# Loop through the array
foreach ($number in $numbersArray) {
    if ($number -gt $inputNumber) {
        # The first number greater than the input is found
        $greaterNumber = $number
        break
    }
}
